apiVersion: v1
kind: Service
metadata:
  name: application-service
  labels:
    app: application-service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3001
    protocol: TCP
    name: http
  selector:
    app: application-service
